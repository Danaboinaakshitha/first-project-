#define IRsensor=7;
#define Relay=9;
Void setup (){
pinMode(7, OUTPUT);
pinMode(9, OUTPUT);
}
Void loop(){
if (digitalWrite(7)==HIGH){ // IR sensor is detected 
digitalWrite (9,HIGH)//relay should ON
}
else {
digitalWrite(9,LOW)// relay should OFF
}
}
